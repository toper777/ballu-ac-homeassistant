[project]
name = "ballu-ac-homeassistant"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "cryptography>=48.0.1",
    "zeroconf>=0.149.16",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
